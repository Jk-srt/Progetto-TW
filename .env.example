# Database Configuration (PostgreSQL - Neon)
# Connection string per database PostgreSQL su Neon
DATABASE_URL=postgresql://neondb_owner:your_password@ep-holy-water-a2lusn2u-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Configurazione alternativa per sviluppo locale (opzionale)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taw_flights
DB_USER=postgres
DB_PASSWORD=your_password

# Backend Configuration
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key
PORT=3000

# Frontend Configuration
ANGULAR_ENV=development
